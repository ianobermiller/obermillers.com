/** Shared PocketBase `users` auth; project data collections are prefixed. */
export const pbCollections = {
  accounts: "familybank_accounts",
  presets: "familybank_presets",
  transactions: "familybank_transactions",
} as const;
