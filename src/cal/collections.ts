/** Shared PocketBase `users` auth; Color Calendar data is prefixed. */
export const calCollections = {
  calendars: "colorcal_calendars",
  categories: "colorcal_categories",
  days: "colorcal_days",
} as const;
