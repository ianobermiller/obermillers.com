name: Setup Node
description: Install Node 24 and npm dependencies
runs:
  using: composite
  steps:
    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: "24"
        cache: npm
    - name: Install
      run: npm ci
      shell: bash
